[pytest]
testpaths = tests
python_files = test_*.py
# src/ sits beside tests/, so pipeline/ must be importable. `python -m pytest`
# adds the cwd to sys.path but the bare `pytest` console script does not.
pythonpath = .
addopts = -q --strict-markers
